@model ProyectommstoreConsumido.Models.Usuarios
@{
    Layout = null;
    ViewBag.Title = "Iniciar Sesión";
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" integrity="sha512-iecdLmaskl7CVkqkXNQ/ZH/XLlvWZOJyj7Yy7tcenmpD1ypASozpmT/E0iPtmFIB46ZmdtAc9eNBvH0H/ZpiBw==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <style>
        body {
            background-image: url('https://images.hdqwalls.com/download/taj-mahal-minimal-art-4k-wu-1920x1080.jpg'); /* Reemplaza con tu imagen si es diferente */
            background-size: cover;
            background-position: center;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: rgba(0, 0, 0, 0.2);
            background-blend-mode: multiply;
        }

        .login-container { /* Usamos un nombre más general */
            background-color: rgba(255, 255, 255, 0.9);
            padding: 40px;
            border-radius: 40px;
            box-shadow: 0 0 30px rgba(0, 0, 0, 0.2);
            text-align: center;
            width: 360px;
            border: 1px solid #ddd;
        }

        h2 {
            color: #333;
            margin-bottom: 30px;
            font-weight: 400;
        }

        .form-group {
            margin-bottom: 20px;
            text-align: left;
            position: relative;
        }

        label {
            display: block;
            margin-bottom: 5px;
            color: #555;
            font-weight: bold;
        }

        .input-group {
            position: relative;
            display: flex;
            align-items: center;
        }

            .input-group i {
                position: absolute;
                left: 0;
                top: 50%;
                transform: translateY(-50%);
                color: #777;
                font-size: 1.1em;
                margin-right: 8px;
            }

        .form-control {
            width: 100%;
            padding: 10px 0;
            border: none;
            border-bottom: 3px solid rgba(0, 0, 0, 0.2);
            border-radius: 0;
            box-sizing: border-box;
            font-size: 16px;
            background-color: transparent;
            color: #333;
            padding-left: 25px; /* Espacio para el icono */
        }

            .form-control::placeholder {
                color: #bbb;
            }

        .btn-login { /* Nuevo nombre para el botón de login */
            background: linear-gradient(90deg, #3f51b5, #e040fb);
            color: white;
            padding: 14px 0;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            font-size: 18px;
            width: 100%;
            transition: background 0.3s ease;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
            margin-top: 20px;
        }

            .btn-login:hover {
                background: linear-gradient(90deg, #283593, #c2185b);
            }

        .register-link { /* Mantenemos el nombre, aunque ahora sea para registrarse */
            color: #777;
            font-size: 0.9em;
            margin-top: 20px;
        }

            .register-link a {
                color: #007bff;
                text-decoration: none;
            }

        .validation-summary-errors {
            color: red;
            text-align: left;
            margin-bottom: 10px;
        }

        .field-validation-error {
            color: red;
            font-size: 0.9em;
            position: absolute;
            top: 100%;
            left: 0;
            width: 100%;
            box-sizing: border-box;
            z-index: 10;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <h2><i class="fas fa-sign-in-alt"></i> Iniciar Sesión</h2>
        @using (Html.BeginForm("LOGIN", "Login", FormMethod.Post, new { @class = "form-horizontal" }))
        {
            @Html.AntiForgeryToken()
            @Html.ValidationSummary(true, "", new { @class = "validation-summary-errors" })

            <div class="form-group">
                <label for="NombreUsuario">Usuario</label>
                <div class="input-group">
                    <i class="fas fa-user"></i>
                    @Html.TextBoxFor(m => m.NombreUsuario, new { @class = "form-control", placeholder = "Tu usuario" })
                    @Html.ValidationMessageFor(m => m.NombreUsuario, "", new { @class = "field-validation-error" })
                </div>
            </div>

            <div class="form-group">
                <label for="password">Contraseña</label>
                <div class="input-group">
                    <i class="fas fa-lock"></i>
                    @Html.PasswordFor(m => m.password, new { @class = "form-control", placeholder = "Tu contraseña" })
                    @Html.ValidationMessageFor(m => m.password, "", new { @class = "field-validation-error" })
                </div>
            </div>

            <button type="submit" class="btn-login"><i class="fas fa-sign-in-alt"></i> Iniciar Sesión</button>

            <div class="register-link">
                ¿No tienes una cuenta? <a href="@Url.Action("Register", "Login")"><i class="fas fa-user-plus"></i> Regístrate</a>
            </div>
        }
    </div>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    @Scripts.Render("~/bundles/jqueryval")
    {}
</body>
</html>